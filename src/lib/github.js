/**
 * GitHub helpers.
 *
 * Thin wrapper around gh CLI commands. Creates repos, creates gists,
 * lists user repos/gists, checks auth status. Used by dev auth,
 * dev config export/import, and backup/restore flows.
 */

// TODO: Implement GitHub helpers
