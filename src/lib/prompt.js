/**
 * Interactive prompts for user input.
 *
 * Automatically skips prompts and returns defaults when detect.js reports
 * a non-interactive caller (ai, ci, pipe).
 */

// TODO: Implement prompt utilities
